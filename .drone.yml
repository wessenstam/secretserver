kind: pipeline
type: exec
name: default

platform:
  os: linux
  
steps:
- name: create file
  commands:
   - cd /home/t320569/github
   - rm -Rf SecureServer
   - git clone https://github.com/wessenstam/SecureServer
