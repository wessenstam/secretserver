kind: pipeline
type: exec
name: default

platform:
  os: linux
  
steps:
- name: create file
  commands:
  - [ -d "SecureServer" ] && git --git-dir=/home/t320569/github/SecureServer || git clone https://github.com/wessenstam/SecureServer
  - chown -R t320569:t320569 SecureServer
  
